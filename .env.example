##############################################################################
# PROJECT NEBULA — Environment Variables
# Copy to .env and fill in real values. Never commit .env to git.
##############################################################################

# --- Environment ----------------------------------------------------------
ENVIRONMENT=production
LOG_LEVEL=INFO

# --- PostgreSQL ------------------------------------------------------------
POSTGRES_USER=nebula
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=nebula_db

# --- AI APIs --------------------------------------------------------------
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=AIza...

# --- Social / Distribution APIs ------------------------------------------
# YouTube Data API v3 — OAuth2 credentials file path (mounted as secret)
# Place your downloaded client_secrets.json at ./secrets/youtube_client_secrets.json
TIKTOK_API_KEY=CHANGE_ME

# --- Celery Worker Tuning -------------------------------------------------
ORCHESTRATOR_CONCURRENCY=4
DSP_CONCURRENCY=2
DSP_CPU_LIMIT=4.0
DSP_MEM_LIMIT=8G
VIDEO_CONCURRENCY=2
FFMPEG_HWACCEL=auto        # auto | nvenc | vaapi | none

# --- CrewAI ---------------------------------------------------------------
CREWAI_VERBOSE=false

# --- Flower Monitoring ----------------------------------------------------
FLOWER_USER=admin
FLOWER_PASSWORD=CHANGE_ME_FLOWER_PASSWORD
